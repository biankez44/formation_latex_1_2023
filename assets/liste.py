liste = [12,24,36,48,60] # Creation de la liste

print(liste) # Affiche l'ensemble de la liste

print(liste[0]) # Affiche la premiere valeurs de la liste (ici 12)
print(liste[1]) # Affiche la deuxieme valeurs de la liste (ici 24)
print(liste[-1]) # Affiche la derniere valeurs de la liste (ici 60)

print(len(liste)) # Affiche la longueur de la liste (ici 5)


liste_valide = [True, 0, 1.2] # Est valide
liste_contenant_une_autre_liste = [0, [True,False], 2.] # Egalement valide